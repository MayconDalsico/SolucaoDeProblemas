export function problem(lines){

  const A = lines[0];
  const B = lines[1];
  const C = lines[2];
  const D = lines[3];

  const diferenca = (Number(A)*Number(B))-(Number(C)*(Number(D)));

  console.log(`DIFERENCA = ${diferenca}`)


}